{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"tab","text":"<p>A CLI tool for viewing, querying, and converting tabular data files. Supports AWS / Azure / Google Cloud Storage URLs.</p>"},{"location":"#supported-formats","title":"Supported Formats","text":"<ul> <li>Jsonl</li> <li>CSV</li> <li>TSV</li> <li>Parquet</li> <li>Avro</li> </ul>"},{"location":"#usage","title":"Usage","text":""},{"location":"#view-data","title":"View data","text":"<p>Display rows from a tabular data file:</p> <p><pre><code>tab view data.csv\n</code></pre> </p> <p>Output to different formats:</p> <pre><code>tab view data.parquet -o jsonl\ntab view data.parquet -o csv\n</code></pre>"},{"location":"#show-schema","title":"Show schema","text":"<pre><code>tab schema data.parquet\n</code></pre>"},{"location":"#show-summary","title":"Show summary","text":"<pre><code>tab summary data.parquet\n</code></pre>"},{"location":"#sql-queries","title":"SQL queries","text":"<p>Run SQL queries on your data. The table is referenced as <code>t</code>:</p> <p><pre><code>tab view --sql 'SELECT * FROM t WHERE Metric_A_Value &gt; 80' test.csv\n</code></pre> </p>"},{"location":"#jmespath-row-reshaping","title":"JMESPath row reshaping","text":"<p>Run a JMESPath expression against each row as a JSON object:</p> <pre><code>tab view ugly.parquet --jp '{id: participant.id, city: profile.address.city}'\ntab cat ugly.parquet --jp 'profile.address.city' -o jsonl\ntab cat ugly.parquet --jp '[participant.id, profile.address.city]' -o jsonl\n</code></pre> <ul> <li>Object results become output columns.</li> <li>Non-object results are written to a single <code>value</code> column.</li> <li>The expression should return a consistent shape across the dataset.</li> <li>Use either <code>--sql</code> or <code>--jp</code>, but not both on the same command.</li> </ul>"},{"location":"#convert","title":"Convert","text":"<p>Convert between formats:</p> <pre><code>tab convert data.csv data.parquet\ntab convert data.parquet data.jsonl -o jsonl\n</code></pre> <p>Write partitioned output:</p> <pre><code>tab convert data.csv output_dir/ -o parquet -n 4\n</code></pre>"},{"location":"#concatenate-multiple-files","title":"Concatenate multiple files","text":"<pre><code>tab cat data1.csv data2.csv data3.csv -o jsonl &gt; output.jsonl\n</code></pre>"},{"location":"#options","title":"Options","text":""},{"location":"#common-options","title":"Common options","text":"Option Description <code>-i</code> Input format (<code>parquet</code>, <code>csv</code>, <code>tsv</code>, <code>jsonl</code>). Auto-detected from extension. <code>-o</code> Output format (<code>parquet</code>, <code>csv</code>, <code>tsv</code>, <code>jsonl</code>). <code>--limit</code> Maximum number of rows to display. <code>--skip</code> Number of rows to skip from the beginning."},{"location":"#convert-options","title":"Convert options","text":"Option Description <code>-n</code> Number of output partitions. Creates a directory with part files."},{"location":"cli-ref/","title":"CLI Reference","text":""},{"location":"cli-ref/#tab-view","title":"<code>tab view</code>","text":"<p>View tabular data from a data file in a rich CLI format, or a directory of partitions of data files.</p> <p>Paths may also use glob patterns such as <code>data/date=*/*.parquet</code> or <code>s3://bucket/path/date=2026-01-*/*</code>.</p> <pre><code>tab view $path [OPTIONS]\n</code></pre> <p>Options:</p> Option Description <code>-i</code> / <code>--input-format</code> Input format (<code>parquet</code>, <code>csv</code>, <code>tsv</code>, <code>jsonl</code>, <code>avro</code>, <code>sqlite</code>, <code>duckdb</code>). Auto-detected from extension if omitted. SQLite and DuckDB input use <code>{url}#{table_name}</code>. <code>--sql</code> SQL query to apply before displaying. The table is available as <code>t</code>. Mutually exclusive with <code>--jmespath</code>. <code>--jmespath</code> / <code>--jp</code> JMESPath expression to apply to each row as JSON. Object outputs become columns; non-object outputs go to a <code>value</code> column. The result shape must stay consistent across rows. <code>--limit</code> Maximum number of rows to display. <code>--skip</code> Number of rows to skip from the beginning. <code>--max-cell-length</code> Truncate cell contents longer than this. If omitted, <code>max_cell_length</code> from config is used when set."},{"location":"cli-ref/#tab-schema","title":"<code>tab schema</code>","text":"<p>Display the schema of a tabular data file.</p> <p>Paths may also use glob patterns.</p> <pre><code>tab schema $path [OPTIONS]\n</code></pre> <p>Options:</p> Option Description <code>-i</code> / <code>--input-format</code> Input format (<code>parquet</code>, <code>csv</code>, <code>tsv</code>, <code>jsonl</code>, <code>avro</code>, <code>sqlite</code>, <code>duckdb</code>). Auto-detected from extension if omitted. SQLite and DuckDB input use <code>{url}#{table_name}</code>."},{"location":"cli-ref/#tab-summary","title":"<code>tab summary</code>","text":"<p>Display summary information about a tabular data file.</p> <p>Paths may also use glob patterns.</p> <pre><code>tab summary $path [OPTIONS]\n</code></pre> <p>Options:</p> Option Description <code>-i</code> / <code>--input-format</code> Input format (<code>parquet</code>, <code>csv</code>, <code>tsv</code>, <code>jsonl</code>, <code>avro</code>, <code>sqlite</code>, <code>duckdb</code>). Auto-detected from extension if omitted. SQLite and DuckDB input use <code>{url}#{table_name}</code>."},{"location":"cli-ref/#tab-convert","title":"<code>tab convert</code>","text":"<p>Convert tabular data from one format to another.</p> <p>Input paths may also use glob patterns when the input format supports multi-file reads.</p> <pre><code>tab convert $src $dst [OPTIONS]\n</code></pre> <p>Options:</p> Option Description <code>-i</code> / <code>--input-format</code> Input format (<code>parquet</code>, <code>csv</code>, <code>tsv</code>, <code>jsonl</code>, <code>avro</code>, <code>sqlite</code>, <code>duckdb</code>). Auto-detected from extension if omitted. SQLite and DuckDB input use <code>{url}#{table_name}</code>. <code>-o</code> / <code>--output-format</code> Output format (<code>parquet</code>, <code>csv</code>, <code>tsv</code>, <code>jsonl</code>, <code>avro</code>). If not specified, inherits from input format. <code>--sql</code> SQL query to apply before writing. The table is available as <code>t</code>. Mutually exclusive with <code>--jmespath</code>. <code>--jmespath</code> / <code>--jp</code> JMESPath expression to apply to each row as JSON. Object outputs become columns; non-object outputs go to a <code>value</code> column. The result shape must stay consistent across rows. <code>-n</code> / <code>--num-partitions</code> Number of output partitions. Creates a directory with partition files."},{"location":"cli-ref/#tab-cat","title":"<code>tab cat</code>","text":"<p>Concatenate tabular data from multiple files.</p> <p>Input paths may also use glob patterns.</p> <pre><code>tab cat $paths [OPTIONS]\n</code></pre> <p>Options:</p> Option Description <code>-i</code> / <code>--input-format</code> Input format (<code>parquet</code>, <code>csv</code>, <code>tsv</code>, <code>jsonl</code>, <code>avro</code>, <code>sqlite</code>, <code>duckdb</code>). Auto-detected from extension if omitted. SQLite and DuckDB input use <code>{url}#{table_name}</code>. <code>-o</code> / <code>--output-format</code> Output format (<code>parquet</code>, <code>csv</code>, <code>tsv</code>, <code>jsonl</code>, <code>avro</code>). If not specified, print Rich table in terminal. <code>--sql</code> SQL query to apply after concatenation. The table is available as <code>t</code>. Mutually exclusive with <code>--jmespath</code>. <code>--jmespath</code> / <code>--jp</code> JMESPath expression to apply to each row as JSON. Object outputs become columns; non-object outputs go to a <code>value</code> column. The result shape must stay consistent across rows."},{"location":"cli-ref/#global-options","title":"Global options","text":"Option Description <code>--az-url-authority-is-account</code> Interpret az:// URL authority as storage account name instead of container name. See Cloud for more information. <code>--log-level</code> Log level from <code>{DEBUG, INFO, WARNING, ERROR, CRITICAL}</code>. If omitted, uses <code>log_level</code> from config."},{"location":"cloud/","title":"Cloud support","text":"<p><code>tab</code> supports directly reading tabular data files or directories from cloud storage:</p> <pre><code>tab view s3://$bucket/$path\ntab view gs://$bucket/$path\ntab view az://$container/$path\ntab view abfss://$container@$account.dfs.core.windows.net/$path\n</code></pre> <p>Cloud paths may also use glob patterns:</p> <pre><code>tab view 's3://$bucket/path/date=*/*.parquet'\ntab view 'gs://$bucket/path/date=2026-01-*/*'\ntab summary 'az://$container/path/date=*/*.jsonl'\n</code></pre> <p>Install all optional integrations with:</p> <pre><code>pip install 'tab-cli[all]'\n</code></pre>"},{"location":"cloud/#aws-s3","title":"AWS S3","text":"<p>Authentication methods (in order):</p> <ol> <li>Environment variables: <code>AWS_ACCESS_KEY_ID</code>, <code>AWS_SECRET_ACCESS_KEY</code>, <code>AWS_SESSION_TOKEN</code></li> <li>Profile: <code>AWS_PROFILE</code> or default \u2014 handles <code>~/.aws/credentials</code>, SSO, assume role, instance metadata</li> </ol> <pre><code># Option 1: Set credentials directly\nexport AWS_ACCESS_KEY_ID=...\nexport AWS_SECRET_ACCESS_KEY=...\n\n# Option 2: Use a profile\naws configure              # static keys\naws sso login              # SSO authentication\nexport AWS_PROFILE=my-profile\n</code></pre>"},{"location":"cloud/#azure-blob-storage","title":"Azure Blob Storage","text":"<p>Authentication methods (in order):</p> <ol> <li>Connection string: <code>AZURE_STORAGE_CONNECTION_STRING</code></li> <li>Account key: <code>AZURE_STORAGE_KEY</code></li> <li>SAS token: <code>AZURE_STORAGE_SAS_TOKEN</code></li> <li>Azure AD / RBAC: <code>DefaultAzureCredential</code></li> <li>Azure CLI: Key fetched via <code>az storage account keys list</code></li> </ol> <pre><code># Option 1: Connection string\nexport AZURE_STORAGE_CONNECTION_STRING=\"DefaultEndpointsProtocol=https;AccountName=...;AccountKey=...\"\n\n# Option 2: Account key\nexport AZURE_STORAGE_ACCOUNT=myaccount\nexport AZURE_STORAGE_KEY=...\n\n# Option 3: SAS token\nexport AZURE_STORAGE_ACCOUNT=myaccount\nexport AZURE_STORAGE_SAS_TOKEN=\"?sv=2022-11-02&amp;ss=...\"\n\n# Option 4: Azure AD (requires RBAC role: Storage Blob Data Reader)\naz login\n\n# Option 5: CLI fallback (requires ARM access)\naz login\n</code></pre>"},{"location":"cloud/#interpretation-of-az-urls","title":"Interpretation of <code>az://</code> URLs","text":"<p>The interpretation of the <code>az://</code> URL authority (the part between <code>az://</code> and the first <code>/</code>) can be configured with the <code>--az-url-authority-is-account</code> flag.</p> <p>Two interpretations are supported:</p> <ul> <li><code>az://$container/$path</code> - the authority is the container name (default adlfs behavior)</li> <li><code>az://$account/$container/$path</code> - the authority is the storage account name</li> </ul> <p>The first form is consistent with <code>s3://</code> and <code>gs://</code> URLs, but requires the <code>AZURE_STORAGE_ACCOUNT</code> environment variable to be set. The second form requires the <code>--az-url-authority-is-account</code> flag. </p>"},{"location":"cloud/#google-cloud-storage","title":"Google Cloud Storage","text":"<p>Authentication methods (in order):</p> <ol> <li><code>GOOGLE_APPLICATION_CREDENTIALS</code>: Path to service account JSON</li> <li>ADC file: <code>~/.config/gcloud/application_default_credentials.json</code></li> <li>gcloud CLI: Token from <code>gcloud auth print-access-token</code></li> <li><code>google.auth.default()</code>: Default credential resolution</li> </ol> <pre><code># Option 1: Service account\nexport GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account.json\n\n# Option 2: User credentials (ADC)\ngcloud auth application-default login\n\n# Option 3: CLI login (fallback)\ngcloud auth login\n</code></pre>"},{"location":"configuration/","title":"Configuration","text":"<p><code>tab</code> supports a global configuration file at <code>~/.config/tab/config.json</code>. Settings in this file serve as defaults and are overridden by CLI flags.</p>"},{"location":"configuration/#setup","title":"Setup","text":"<p>Create the config file:</p> <pre><code>mkdir -p ~/.config/tab\ncat &gt; ~/.config/tab/config.json &lt;&lt; 'EOF'\n{\n  \"az_url_authority_is_account\": false,\n  \"default_num_view_rows\": 20,\n  \"log_level\": \"INFO\",\n  \"max_cell_length\": null,\n  \"num_remote_workers\": 8,\n  \"sampling_size_for_schema_inference\": 32\n}\nEOF\n</code></pre>"},{"location":"configuration/#available-settings","title":"Available settings","text":"Key Type Default Description <code>az_url_authority_is_account</code> <code>bool</code> <code>false</code> Interpret <code>az://</code> URL authority as storage account name instead of container name. <code>default_num_view_rows</code> <code>int</code> <code>20</code> Default number of rows shown by <code>tab view</code> when <code>--limit</code> is omitted. <code>log_level</code> <code>str</code> <code>\"INFO\"</code> Default CLI log level when <code>--log-level</code> is omitted. <code>max_cell_length</code> <code>int \\| null</code> <code>null</code> Default maximum cell length for <code>tab view</code>. The CLI <code>--max-cell-length</code> flag overrides it. <code>num_remote_workers</code> <code>int</code> <code>8</code> Maximum worker threads for remote per-partition summary work such as Parquet row counts. <code>sampling_size_for_schema_inference</code> <code>int</code> <code>32</code> Number of rows sampled for schema inference (e.g. when using <code>--jp</code>)."},{"location":"configuration/#precedence","title":"Precedence","text":"<p>Settings are applied in this order (last wins):</p> <ol> <li>Built-in defaults</li> <li>Config file (<code>~/.config/tab/config.json</code>)</li> <li>CLI flags (e.g. <code>--az-url-authority-is-account</code>)</li> </ol> <p>If the config file does not exist, built-in defaults are used. Unknown keys in the file are ignored with a warning.</p>"},{"location":"databases/","title":"Databases","text":"<p><code>tab</code> can read SQLite and DuckDB tables as tabular input.</p>"},{"location":"databases/#install","title":"Install","text":"<p>Database support is optional:</p> <pre><code>pip install 'tab-cli[sqlite]'\npip install 'tab-cli[duckdb]'\npip install 'tab-cli[all]'\n</code></pre> <p>If a dependency is missing, <code>tab</code> will raise an error telling you which extra to install.</p>"},{"location":"databases/#input-syntax","title":"Input Syntax","text":"<p>SQLite and DuckDB input use this form:</p> <pre><code>{url}#{table_name}\n</code></pre> <p>The <code>#table_name</code> suffix is required.</p>"},{"location":"databases/#sqlite","title":"SQLite","text":"<p>Examples:</p> <pre><code>tab view data.db#users\ntab schema data.sqlite#events\ntab summary data.sqlite3#measurements\n</code></pre> <p>SQLite file extension inference supports:</p> <ul> <li><code>.db</code></li> <li><code>.sqlite</code></li> <li><code>.sqlite3</code></li> </ul>"},{"location":"databases/#duckdb","title":"DuckDB","text":"<p>Examples:</p> <pre><code>tab view data.duckdb#users\ntab schema warehouse.duckdb#events\ntab summary analytics.ddb#measurements\n</code></pre> <p>DuckDB file extension inference supports:</p> <ul> <li><code>.duckdb</code></li> <li><code>.ddb</code></li> </ul>"},{"location":"databases/#local-and-remote-files","title":"Local And Remote Files","text":"<p>Both SQLite and DuckDB input work with any storage backend supported by <code>tab</code>.</p> <p>Examples:</p> <pre><code>tab view /tmp/data.db#users\ntab view s3://bucket/path/data.db#users\ntab view gs://bucket/path/data.sqlite#users\ntab view az://container/path/data.sqlite3#users\n\ntab view /tmp/data.duckdb#users\ntab view s3://bucket/path/data.duckdb#users\ntab view gs://bucket/path/data.ddb#users\ntab view az://container/path/data.duckdb#users\n</code></pre> <p>For local files, <code>tab</code> queries the database directly.</p> <p>For remote files, <code>tab</code> downloads the database file to a temporary local file and then queries it with Polars.</p>"},{"location":"databases/#supported-commands","title":"Supported Commands","text":"<p>SQLite and DuckDB are supported as input for:</p> <pre><code>tab view data.db#users\ntab schema data.db#users\ntab summary data.db#users\ntab cat data.db#users -o csv\ntab convert data.db#users output.jsonl -o jsonl\n\ntab view data.duckdb#users\ntab schema data.duckdb#users\ntab summary data.duckdb#users\ntab cat data.duckdb#users -o csv\ntab convert data.duckdb#users output.jsonl -o jsonl\n</code></pre>"},{"location":"databases/#output-behavior","title":"Output Behavior","text":"<p>SQLite and DuckDB are input-only.</p> <p><code>tab</code> does not write SQLite or DuckDB output, so commands that need a streamed output format must use <code>-o</code> explicitly:</p> <pre><code>tab cat data.db#users -o csv\ntab cat data.db#users -o jsonl\n\ntab cat data.duckdb#users -o csv\ntab cat data.duckdb#users -o jsonl\n</code></pre> <p>This is required because SQLite and DuckDB are database containers, not streamable stdout formats.</p>"},{"location":"databases/#notes","title":"Notes","text":"<ul> <li>Database input must reference a single file, not a directory or glob.</li> <li>The table name is treated as a table identifier, not a free-form SQL query.</li> </ul>"}]}